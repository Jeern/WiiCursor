namespace DrawCursor
{
    /// <summary>
    /// The different states of the desktop.
    /// In the application the state is toggled by pressing Ctrl
    /// </summary>
    public enum DrawState
    {
        TransparentCanDrawNoDesktopInteraction,
        SemitransparentDrawingFixed,
        TransparentNoDrawing
    }
}
